# T-0001 — Repo scaffold + determinism guard

**Day:** Sat 29 Aug · **Risk:** low · **Cuttable:** no — spine · **Depends on:** none

## Context to read
`CLAUDE.md` (repo layout section). Nothing else — this ticket names its own context.

## Build
- `pyproject.toml` — Python ≥3.11, MIT/BSD/Apache-2.0 dependencies only
- `src/rakshak/config.py` — global `SEED = 42`, all paths, cost-matrix defaults (provisional, see `07-math.md §5`)
- `Makefile` — `setup` / `eval` / `figures` / `test` targets (stubs are fine, they must run and exit 0)
- `tests/` scaffold + pytest config
- Every script that will exist later must accept `--seed`; enforce the convention now in a shared arg-parser helper

## Open branch to check
STATE.md flags: "Rakshak legacy code — prior work under this name was not recoverable at planning time. If it surfaces, salvage the eval harness only." Check `../ver1/` before writing from scratch — if a usable `eval/` skeleton exists there, salvage it; the transaction-level framing is superseded either way.

## Done when
- `make setup` runs clean on a fresh checkout
- `python -c "from rakshak.config import SEED"` succeeds
- `pytest` collects with 0 errors (empty/smoke test is fine)
- Any dependency added is recorded in `LOGBOOK.md` with its licence
