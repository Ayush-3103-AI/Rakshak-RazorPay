# T-0007 — SUPERSEDED, split into T-0007a and T-0007b on 2026-08-28

This ticket was split during the spec revision that followed T-0006. It was the heaviest
remaining ticket and it carried two separable deliverables, the first of which is
independently verifiable and unblocks the primary metric on its own.

| Successor | Scope |
|---|---|
| [T-0007a](T-0007a.md) | Cost redefinition (`c_fp` lifetime value, `L_m` realised loss) + the oracle-dominance invariant. Makes `savings` readable. |
| [T-0007b](T-0007b.md) | BMR three-action policy, capacity constraint, and the cost-asymmetry sweep machinery. |

Nothing was dropped in the split. The original scope — `policy.py`, `cost.py`, the
capacity constraint, FR-015/016/017 — is fully covered across the two, plus the two
additions the revision introduced: the corrected cost definitions and the sweep that
`00-charter.md §2` now requires.

**Do not execute this file.** It is kept so the board's history reads straight.
