# T-0010 — BOCPD changepoint baseline

**Day:** Mon 31 Aug–1 Sep · **Risk:** low · **Cuttable: yes — cut 2nd** · **Depends on:** T-0005, T-0007

## Context to read
Stack table in `CLAUDE.md` — hand-written BOCPD, Adams & MacKay (2007), truncated run-length posterior. `06-requirements.md §3`, "Additional baselines" row: BOCPD + identical cost layer, isolates HMM vs. changepoint detection.

## Build — `src/rakshak/models/bocpd.py`
- Truncated run-length posterior per Adams & MacKay 2007
- Wire the **same** T-0007 cost layer on top, unmodified, so the comparison against the HMM is apples-to-apples

## Done when
- BOCPD + cost layer produces a comparable row in `results/summary.md` next to the HMM and LightGBM rows

## Cut note
If dropped, the ablation table (T-0011) simply has one fewer baseline row — say so, don't backfill with a fake number.
