# T-0023 — Drift-detection literature survey

**Day:** Sun 30 Aug (evening) → Mon 31 Aug · **Risk:** low — doc only, no code touched · **Cuttable: yes — ranks below T-0013/T-0018/T-0020/T-0021/T-0019, see BOARD.md cut order** · **Depends on (blocked by):** none — can start immediately, independent of T-0022a/b/c

## Context to read

`project-context/14-spec-blackswan-and-drift-survey.md`. `project-context/12-lit-survey-k1.md` (the format and discipline to match). `CLAUDE.md`'s stack table and rejection table. `docs/adr/ADR-0001` through `ADR-0009`.

## Why this ticket exists

Aside from the K1 survey (which addressed a narrow metric-choice question — ARI vs. AMI), nothing has re-checked the locked stack against current prior art specifically on this project's actual problem: post-onboarding merchant/account risk drift detection sitting between transaction-level and onboarding-time scoring. The stack has gone unchallenged since the ADRs were written, not necessarily unchallengeable.

## Build

- Survey prior art on: sequence models over merchant transaction streams, changepoint detection for behavioral drift, bust-out detection, laundering-endpoint/mule-account detection, refund-collusion detection — evaluated against this project's hard constraints (CPU only, solo, originally a 4-day build, now with ~1 day of extension).
- For every locked stack choice and every rejected alternative in `CLAUDE.md`'s stack table and ADR-0001 through ADR-0009, render one verdict: **reaffirmed** (literature still supports it), **reaffirmed with a caveat** (supports it, but a newer method exists that wasn't feasible under this project's constraints — name it), or **reconsidered** (literature suggests the decision should change).
- Write `project-context/15-lit-survey-drift-detection.md`, following `12-lit-survey-k1.md`'s format (question, sources checked, verdict, what it changed).
- For any **reconsidered** finding, append a dated addendum block to the relevant `docs/adr/ADR-000X-*.md` file, in that file's own house format — never a silent rewrite.
- Hand a short paragraph summarizing the verdict to whoever executes T-0013, for its limitations/future-work section.

## Done when

- `project-context/15-lit-survey-drift-detection.md` exists and every claim about the current stack cites the specific ADR or `CLAUDE.md` line it agrees or disagrees with — checkable by a reader, not asserted from memory.
- Every ADR from 0001 to 0009 has an explicit verdict recorded (reaffirmed / reaffirmed-with-caveat / reconsidered).
- Any reconsidered ADR carries a dated addendum, not a rewrite.
- A short paragraph is ready to hand to T-0013.

## What this ticket must NOT do

- Must not touch `src/`, `MODEL_REGISTRY`, or any file under `results/`.
- Must not implement or benchmark a new candidate model, even if the survey turns up a compelling one — that was explicitly proposed and explicitly rejected as too risky for a one-night, honest-metrics-required build. A compelling finding becomes the headline of "future work," not new code.
- Must not rewrite or delete any existing ADR's original text.
