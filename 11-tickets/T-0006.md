# T-0006 — Baselines: rule engine, LightGBM, random

**Day:** Sun 30 Aug · **Risk: sets up A-005 (does the HMM even earn its place?)** · **Cuttable:** no — spine · **Depends on:** T-0004, T-0005

## Context to read
`06-requirements.md §3` (Floor + Additional baselines table).

## Build — `src/rakshak/models/`
- `rules.py` — the floor: flag if 7-day transaction velocity > 3× the merchant's trailing 90-day mean, OR refund ratio > 15%, OR chargeback ratio > 1%. Fixed global thresholds, no learning.
- `gbdt.py` — LightGBM on windowed aggregates, no HMM. This isolates whether latent-state modelling earns its place at all (A-005).
- Random-selection baseline at equal review budget — the absolute floor, for sanity-checking that anything beats chance.

## Done when
- All three baselines run end-to-end through the T-0005 eval harness and each produces one row in `results/summary.md`
- No verdict is being rendered yet — this ticket is plumbing. The comparison that matters happens at T-0011.
