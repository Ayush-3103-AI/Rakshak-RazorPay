# T-0022c — Black-swan report

**Day:** Mon 31 Aug · **Risk:** medium — real execution risk on a one-night build, has a pre-agreed fallback · **Cuttable: yes — ranks below T-0013/T-0018/T-0020/T-0021/T-0019, see BOARD.md cut order** · **Depends on (blocked by):** T-0022a, T-0022b

## Context to read

`project-context/14-spec-blackswan-and-drift-survey.md`, especially "Fallback definition" under Implementation Decisions. `results/verdict.md` (K2's FAIL) and `results/summary.md` (the `render_summary` style to match).

## Why this ticket exists

This is where the black-swan track answers its actual research question: does a population-wide shared shock cause `rules`/`gbdt`/`hmm` to falsely flag healthy merchants as drifting — validating or complicating this project's own central cost claim that blunt global thresholds freeze honest merchants?

## Build — primary path

- Use T-0022a's shock injection to produce a full `data/synthetic_shock/` dataset with the shock present across train/validate/test (so refit models can potentially learn to tolerate a shared shock, not just be surprised by it once).
- Via T-0022b's seam, refit `rules`/`gbdt`/`hmm` on the shocked dataset's own train/validate.
- Score shock-day windows vs. matched non-shock control windows from the same merchants. Report flagged-fraction (and review/hold rate where applicable) per model, as a delta **with the underlying counts shown** — never a bare delta, matching the `random`-floor discipline used everywhere else in this repo (AP-06).
- Write `results/blackswan.md` using the `render_summary` pattern from `eval/harness.py` — same table style, same provenance line (seed, dataset path, split) at the top.

## Build — fallback path (use if the primary refit is at risk of not landing by Mon 31 Aug night)

This repo has no model-persistence machinery, so "reuse the already-fitted T-0011 models" is not a lighter fallback — it would require building new infrastructure. The actual lighter fallback:

- Leave `split.train` / `split.validate` exactly as the frozen `data/synthetic/` run — nothing about how the models are fit changes.
- Inject the shock only into the transactions underlying `split.test`'s feature windows before scoring (a copy, not a mutation of the frozen file).
- Report the same flagged-fraction-under-shock-vs-control metric. State plainly in `results/blackswan.md` which path (primary or fallback) produced the numbers — do not let a reader assume full retrain happened if it didn't.

## Done when

- `results/blackswan.md` exists, states which path (primary/fallback) it used, and reports flagged-fraction under shock vs. control for `rules`/`gbdt`/`hmm` with counts shown.
- The result is reported honestly whichever way it falls — including if it shows Rakshak's own models are just as prone to shock-induced false positives as a blunt global threshold would be.
- No existing file under `results/` (`verdict.md`, `ablations.md`, `baf_validation.md`, `lag_probe.md`, `summary.md`, `sensitivity.md`) changes.
- `python -m pytest` and `python -m ruff check src tests` still pass.

## What this ticket must NOT do

- Must not be framed as strengthening, rescuing, or rebutting K2's FAIL verdict — it is an orthogonal robustness probe.
- Must not touch `00-charter.md`'s pre-registered NFR-001 bar or `results/verdict.md`'s wording.
- Must not silently substitute the fallback for the primary without saying so in `results/blackswan.md`.
