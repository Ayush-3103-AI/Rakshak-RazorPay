# T-0008 — Empirical-Bayes shrinkage

**Day:** Sun 30 Aug · **Risk:** low · **Cuttable: yes — cut 4th** · **Depends on:** T-0004, T-0007

## Context to read
ADR-0006 (empirical-Bayes shrinkage, closed form — no MCMC, no PyMC), NFR-007, FR-011 (segment definitions).

## Build — `src/rakshak/decision/shrinkage.py`
- Closed-form empirical-Bayes per-merchant threshold calibration: a merchant's threshold shrinks toward its segment's (MCC × AOV-band) threshold in proportion to how little history it has
- Cold-start handling: merchants below the transaction floor get the segment default, explicitly logged as such — never a silent guess

## Done when
- `tests/test_shrinkage.py`: a merchant with ≥20 transactions receives a segment-shrunk threshold; a merchant below 20 gets the segment default, and that fallback is logged (NFR-007, exact acceptance text)

## Cut note
If dropped, T-0007's policy falls back to segment-level thresholds with no per-merchant calibration — degraded but functional, not broken.
