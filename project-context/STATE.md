# STATE.md lives at the repo root

Session state is **`../STATE.md`**, at the repository root. Read that one.

This file exists only because `CLAUDE.md` and several ticket files pointed here for a state file
that was never created at this path. Rather than keep a second state file that would drift out of
sync with the real one, T-0017 (2026-08-28) fixed the pointer in `CLAUDE.md` and left this
redirect for the ticket files it does not own.

`project-context/` holds surveys and long-form background — see `12-lit-survey-k1.md`.
